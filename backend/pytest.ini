[pytest]
asyncio_mode = auto
testpaths = tests
addopts =
    --tb=short
    --import-mode=importlib
    --cov=app
    --cov=mcp_server
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-fail-under=40
