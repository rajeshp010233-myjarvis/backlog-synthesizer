[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts =
    --tb=short
    --import-mode=importlib
    --cov=app
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-fail-under=25
